# ======================================================================
# Cucumber / JUnit Platform execution settings
#
# Parallel execution is off by default: OLX is a live production site and
# driving several browsers at it from one IP invites rate limiting. Turn it
# on for suites that target your own environments.
#
#   mvn test -Dcucumber.execution.parallel.enabled=true
#
# The framework itself is parallel-safe: DriverManager holds the WebDriver in
# a ThreadLocal and Hooks clears it after every scenario.
# ======================================================================

cucumber.execution.parallel.enabled=false
cucumber.execution.parallel.config.strategy=fixed
cucumber.execution.parallel.config.fixed.parallelism=2
cucumber.execution.parallel.config.fixed.max-pool-size=4

# Fail the build on undefined or pending steps rather than reporting a pass.
cucumber.execution.strict=true

cucumber.snippet-type=camelcase
